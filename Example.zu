-- | Defines natural numbers without induction feature, it's
-- like functions in dependent langauges.
nat : _ = \pi (N : \type) -> (N -> N) -> N -> N.

-- | Defines the zero constructor
Zero : _ = \fun _, _, zero
  zero.

-- | Defines the succ constructor
Succ : _ = \fun n, N, succ, _
  (n N succ zero).
